export type Service = {
  id: number;
  slug: string;
  translationKey: string;
  image: string;
};

export type ContactFormValues = {
  name: string;
  phone: string;
  email: string;
  message: string;
};
